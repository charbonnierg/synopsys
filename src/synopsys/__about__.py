"""The `__about__` module exposes the version of the `synopsys` package:

Example:

```python
from synopsys.__about__ import __version__
print(__version__)
```

Note that container version can also be imported directly from `synopsys` package:

Example:

```python
from synopsys import __version__
print(__version__)
```
"""
__version__ = "0.1.0"
